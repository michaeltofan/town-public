{
  "name": "town-public-e2e",
  "private": true,
  "version": "0.1.0",
  "description": "Playwright E2E for town-public (kept out of site root so Railway static deploy stays intact)",
  "scripts": {
    "test:e2e": "playwright test",
    "test:e2e:install": "playwright install chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.54.2"
  }
}
